{
  "name": "@lobocon/prisma-client",
  "version": "1.0.0",
  "main": "src/index.ts",
  "type": "commonjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate": "prisma generate --schema ./schema.prisma",
    "build": "tsc",
    "postinstall": "npm run generate && npm run build"
  },
  "keywords": [],
  "author": "Frank",
  "license": "MIT",
  "description": "Prisma Client for Lobocon",
  "dependencies": {
    "@prisma/client": "^6.16.3",
    "prisma": "^6.16.3"
  },
  "devDependencies": {
    "@types/node": "^24.7.0",
    "ts-node": "^10.9.2"
  }
}
